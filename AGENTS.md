# Agent Guidelines

## Development workflow

This project uses **trunk-based development**. Do all work directly on `main`:

- Commit small, coherent changes straight to `main` and keep it releasable.
- Do **not** create feature branches unless the user explicitly asks for one.
- Pull/fast-forward `main` before starting and before pushing.
